# Challenge #1
# Create a Python script that removes all the elements of a list that are duplicates.
# Do the challenge in a single line of code using sets.
list_set = list(set([1, 2, 3, 4, 1, 2, 2, 1]))
print(list_set)